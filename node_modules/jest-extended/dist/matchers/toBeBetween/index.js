"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeBetween = toBeBetween;

var _jestMatcherUtils = require("jest-matcher-utils");

var _predicate = _interopRequireDefault(require("./predicate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const passMessage = (received, startDate, endDate) => () => (0, _jestMatcherUtils.matcherHint)('.not.toBeBetween', 'received', '') + '\n\n' + `Expected date to be between ${(0, _jestMatcherUtils.printReceived)(startDate)} and ${(0, _jestMatcherUtils.printReceived)(endDate)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

const failMessage = (received, startDate, endDate) => () => (0, _jestMatcherUtils.matcherHint)('.toBeBetween', 'received', '') + '\n\n' + `Expected date to be between ${(0, _jestMatcherUtils.printReceived)(startDate)} and ${(0, _jestMatcherUtils.printReceived)(endDate)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

function toBeBetween(date, startDate, endDate) {
  const pass = (0, _predicate.default)(date, startDate, endDate);

  if (pass) {
    return {
      pass: true,
      message: passMessage(date, startDate, endDate)
    };
  }

  return {
    pass: false,
    message: failMessage(date, startDate, endDate)
  };
}