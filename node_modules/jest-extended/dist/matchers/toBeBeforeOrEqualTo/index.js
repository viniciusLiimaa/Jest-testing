"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeBeforeOrEqualTo = toBeBeforeOrEqualTo;

var _jestMatcherUtils = require("jest-matcher-utils");

var _predicate = _interopRequireDefault(require("./predicate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const passMessage = (received, before) => () => (0, _jestMatcherUtils.matcherHint)('.not.toBeBeforeOrEqualTo', 'received', '') + '\n\n' + `Expected date to be before or equal to ${(0, _jestMatcherUtils.printReceived)(before)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

const failMessage = (received, before) => () => (0, _jestMatcherUtils.matcherHint)('.toBeBeforeOrEqualTo', 'received', '') + '\n\n' + `Expected date to be before or equal to ${(0, _jestMatcherUtils.printReceived)(before)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

function toBeBeforeOrEqualTo(date, before) {
  const pass = (0, _predicate.default)(date, before);

  if (pass) {
    return {
      pass: true,
      message: passMessage(date, before)
    };
  }

  return {
    pass: false,
    message: failMessage(date, before)
  };
}