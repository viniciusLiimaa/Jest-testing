"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toBeAfterOrEqualTo = toBeAfterOrEqualTo;

var _jestMatcherUtils = require("jest-matcher-utils");

var _predicate = _interopRequireDefault(require("./predicate"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const passMessage = (received, before) => () => (0, _jestMatcherUtils.matcherHint)('.not.toBeAfterOrEqualTo', 'received', '') + '\n\n' + `Expected date to be after or equal to ${(0, _jestMatcherUtils.printReceived)(before)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

const failMessage = (received, before) => () => (0, _jestMatcherUtils.matcherHint)('.toBeAfterOrEqualTo', 'received', '') + '\n\n' + `Expected date to be after or equal to ${(0, _jestMatcherUtils.printReceived)(before)} but received:\n` + `  ${(0, _jestMatcherUtils.printReceived)(received)}`;

function toBeAfterOrEqualTo(date, after) {
  const pass = (0, _predicate.default)(date, after);

  if (pass) {
    return {
      pass: true,
      message: passMessage(date, after)
    };
  }

  return {
    pass: false,
    message: failMessage(date, after)
  };
}